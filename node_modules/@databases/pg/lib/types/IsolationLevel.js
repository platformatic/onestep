"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isolationLevelToString = void 0;
const assert_never_1 = require("assert-never");
/**
 * The default level is "READ_COMMITTED". "READ_COMMITTED" and "READ_UNCOMMITTED"
 * are equivalent in postgres.
 *
 * For more information, see: https://www.postgresql.org/docs/9.6/transaction-iso.html
 */
var IsolationLevel;
(function (IsolationLevel) {
    IsolationLevel["READ_UNCOMMITTED"] = "READ_UNCOMMITTED";
    IsolationLevel["READ_COMMITTED"] = "READ_COMMITTED";
    IsolationLevel["REPEATABLE_READ"] = "REPEATABLE_READ";
    IsolationLevel["SERIALIZABLE"] = "SERIALIZABLE";
})(IsolationLevel || (IsolationLevel = {}));
exports.default = IsolationLevel;
function isolationLevelToString(level) {
    switch (level) {
        case IsolationLevel.READ_UNCOMMITTED:
            return 'ISOLATION LEVEL READ UNCOMMITTED';
        case IsolationLevel.READ_COMMITTED:
            return 'ISOLATION LEVEL READ COMMITTED';
        case IsolationLevel.REPEATABLE_READ:
            return 'ISOLATION LEVEL REPEATABLE READ';
        case IsolationLevel.SERIALIZABLE:
            return 'ISOLATION LEVEL SERIALIZABLE';
        default:
            void (0, assert_never_1.default)(level, true);
            throw new Error(`${level} is not a valid isolation level`);
    }
}
exports.isolationLevelToString = isolationLevelToString;
//# sourceMappingURL=IsolationLevel.js.map