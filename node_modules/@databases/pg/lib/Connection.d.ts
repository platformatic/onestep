/// <reference types="node" />
import { Readable } from 'stream';
import { BaseConnection } from '@databases/shared';
import { SQLQuery } from '@databases/sql';
import { Connection as IConnection } from './types/Queryable';
import PgDriver from './Driver';
import Transaction from './Transaction';
export default class Connection extends BaseConnection<Transaction, PgDriver> implements IConnection {
    readonly sql: import("@databases/sql").SQL;
    queryNodeStream(query: SQLQuery, options?: {
        highWaterMark?: number;
    }): Readable;
}
